import { combineReducers } from 'redux';
import produce from "immer";
import Papa from 'papaparse';


const app = (state = {
  activeElements: []
}, action) => {
  switch (action.type) {
    default:
      return state
  }

}

export default app
